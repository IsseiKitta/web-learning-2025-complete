<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/taskAdd.css" />
    <title>TaskMaster - Add New Task</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="task-add">
      <header class="task-add__header">
        <h1 class="task-add__header-title">Add New Task</h1>
      </header>

      <form method="post" action="/tasks/add" class="task-add__form">
        <div class="task-add__form-group">
          <label for="title" class="task-add__label">Title</label>
          <input
            type="text"
            id="title"
            name="title"
            placeholder="Enter task title"
            class="task-add__input"
            required
          />
        </div>

        <div class="task-add__form-group">
          <label for="description" class="task-add__label">Description</label>
          <textarea
            id="description"
            name="description"
            placeholder="Enter task description"
            class="task-add__textarea"
            rows="4"
          ></textarea>
        </div>

        <div class="task-add__form-group">
          <label for="dueDate" class="task-add__label">Due Date</label>
          <input
            type="date"
            id="dueDate"
            name="dueDate"
            class="task-add__input"
          />
        </div>

        <div class="task-add__form-group">
          <label for="status" class="task-add__label">Status</label>
          <select id="status" name="status" class="task-add__select">
            <option value="Not Started">Not Started</option>
            <option value="In Progress">In Progress</option>
            <option value="Completed">Completed</option>
          </select>
        </div>

        <div class="task-add__buttons">
          <a href="/tasks" class="task-add__cancel-button">Cancel</a>
          <button type="submit" class="task-add__create-button">
            Create Task
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
